enum Register { a, b, c, d, e, f, g, h };

enum var_type{ NUM, VAR, ARR, PROC };

enum cond_type{ eq, neq, gt, lt, geq, leq };

inline long long int line_offset = 0;
inline long long int last_mem = 0;