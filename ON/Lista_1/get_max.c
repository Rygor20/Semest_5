//Igor Misiek
//Obliczenia naukowe - Lista 1

#include <stdio.h>
#include <float.h>

int main() {
    printf("Maksymalna wartość dla Float16 (half): C nie obsługuje\n");
    printf("Maksymalna wartość dla Float32 (single): %e\n", FLT_MAX);
    printf("Maksymalna wartość dla Float64 (double): %e\n", DBL_MAX);

    return 0;
}
